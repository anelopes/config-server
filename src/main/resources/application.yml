server:
  port: 8888

spring:
  cloud:
    config:
      server:
        encrypt:
          enabled: false
        git:
          uri: https://github.com/anelopes/ms-properties/
          search-paths: provider, store, transporter